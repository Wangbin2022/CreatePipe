<Window x:Class="CreatePipe.OfficalSamples.FoundationSlabView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
       Title="基础筏板生成工具" Height="650" Width="700"
        WindowStartupLocation="CenterScreen"
        xmlns:conv="clr-namespace:CreatePipe.Form.Converters">

    <Window.Resources>
        <conv:InverseBooleanConverter   x:Key="InverseBooleanConverter"/>
        <Style x:Key="GroupBoxStyle" TargetType="GroupBox">
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 图形预览区域 -->
        <GroupBox Grid.Row="0" Header="楼板轮廓预览" Style="{StaticResource GroupBoxStyle}">
            <Border BorderBrush="Black" BorderThickness="1" Background="Black"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Image Source="{Binding PreviewImage}" 
                       Stretch="Uniform"
                       UseLayoutRounding="True"/>
            </Border>
        </GroupBox>

        <!-- 楼板列表区域 -->
        <GroupBox Grid.Row="1" Header="可转换的楼板列表" Style="{StaticResource GroupBoxStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="全选" Width="70" Margin="0,0,5,0" Command="{Binding SelectAllCommand}"/>
                    <Button Content="清空" Width="70" Command="{Binding ClearAllCommand}"/>
                    <TextBlock Text="已选择: " Margin="15,5,0,0"/>
                    <TextBlock Text="{Binding SelectedCount}" FontWeight="Bold"/>
                </StackPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Slabs}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Extended">

                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="选择" Binding="{Binding IsSelected}" Width="50"/>
                        <DataGridTextColumn Header="标记" Binding="{Binding Mark}" Width="80"/>
                        <DataGridTextColumn Header="标高" Binding="{Binding LevelName}" Width="100"/>
                        <DataGridTextColumn Header="类型" Binding="{Binding SlabTypeName}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- 基础筏板类型选择 -->
        <GroupBox Grid.Row="2" Header="基础筏板类型" Style="{StaticResource GroupBoxStyle}">
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock Text="选择基础筏板类型：" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox ItemsSource="{Binding FoundationTypes}"
                          SelectedItem="{Binding SelectedFoundationType}"
                          DisplayMemberPath="Name"
                          Width="200"
                          IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}"/>
            </StackPanel>
        </GroupBox>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="10,0,10,10">
            <Button Content="创建基础筏板" 
                    Width="120" Height="32" 
                    Command="{Binding CreateCommand}"
                    IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}" />

            <Button Content="刷新" 
                    Width="80" Height="32" Margin="10,0,0,0"
                    Command="{Binding RefreshCommand}"/>

            <Button Content="关闭" 
                    Width="80" Height="32" Margin="10,0,0,0"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
