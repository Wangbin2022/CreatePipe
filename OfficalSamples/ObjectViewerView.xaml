<Window x:Class="CreatePipe.OfficalSamples.ObjectViewerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
 Title="对象查看器" Height="650" Width="950"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        xmlns:conv="clr-namespace:CreatePipe.Form.Converters">

    <Window.Resources>
        <conv:EnumToBooleanConverter  x:Key="EnumToBoolConverter"/> 
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题信息 -->
        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Text="{Binding ElementInfo}" 
                   FontWeight="Bold" Margin="0,0,0,5"/>

        <!-- 预览区域 -->
        <Border Grid.Row="1" Grid.Column="0" 
                BorderBrush="Gray" BorderThickness="1"
                Background="White" CornerRadius="3">
            <Image x:Name="PreviewImage" Stretch="Fill"/>
        </Border>

        <!-- 控制面板 -->
        <GroupBox Grid.Row="1" Grid.Column="2" 
                  Header="预览设置" Margin="10,0,0,0">
            <StackPanel Margin="10">
                <!-- 显示类型 -->
                <TextBlock Text="显示类型：" FontWeight="Bold" Margin="0,0,0,5"/>
                <RadioButton Content="物理模型" 
                             IsChecked="{Binding DisplayKind, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=GeometryModel}"
                             Margin="15,2"/>
                <RadioButton Content="分析模型" 
                             IsChecked="{Binding DisplayKind, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=AnalyticalModel}"
                             Margin="15,10"/>

                <!-- 分隔线 -->
                <Separator Margin="0,10"/>

                <!-- 详图级别 -->
                <TextBlock Text="详图级别：" FontWeight="Bold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding DetailLevels}"
                          SelectedItem="{Binding DetailLevel}"
                          DisplayMemberPath="DisplayName"
                          Margin="0,0,0,10"/>

                <!-- 视图选择 -->
                <TextBlock Text="显示视图：" FontWeight="Bold" Margin="0,0,0,5"/>
                <ListBox ItemsSource="{Binding AvailableViews}"
                         SelectedItem="{Binding SelectedView}"
                         DisplayMemberPath="Name"
                         Height="120"
                         Margin="0,0,0,10"/>

                <!-- 视角方向 -->
                <TextBlock Text="视角方向：" FontWeight="Bold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding ViewDirections}"
                          SelectedItem="{Binding CurrentViewDirection}"
                          Margin="0,0,0,10"/>
            </StackPanel>
        </GroupBox>

        <!-- 参数表格 -->
        <GroupBox Grid.Row="1" Grid.Column="1" 
                  Header="参数" Margin="5,0,5,0"
                  Width="250">
            <DataGrid ItemsSource="{Binding Parameters}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      RowHeight="25">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="参数名" 
                                        Binding="{Binding Name}"
                                        Width="100"/>
                    <DataGridTextColumn Header="值" 
                                        Binding="{Binding Value}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="确定" Command="{Binding OkCommand}"
                    Width="75" Margin="5" IsDefault="True"/>
            <Button Content="取消" Command="{Binding CancelCommand}"
                    Width="75" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
