<Window x:Class="CreatePipe.OfficalSamples.MaterialPropertiesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
        Title="Material Properties" 
        Height="600" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 材料类型选择行 -->
        <Grid Grid.Row="0" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Material Type:" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" 
                      ItemsSource="{Binding MaterialTypes}"
                      SelectedItem="{Binding SelectedMaterialType}"
                      IsEditable="False"/>
        </Grid>

        <!-- 子材料类型选择行 -->
        <Grid Grid.Row="1" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Sub Material:" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding SubMaterials}"
                      SelectedItem="{Binding SelectedSubMaterial}"
                      DisplayMemberPath="Name"
                      IsEnabled="{Binding IsSubTypeEnabled}"
                      IsEditable="False"/>
        </Grid>

        <!-- 参数表格 -->
        <DataGrid Grid.Row="2" 
                  Margin="0,10"
                  ItemsSource="{Binding Parameters}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Parameter" 
                                    Binding="{Binding ParameterName}"
                                    Width="*"/>
                <DataGridTextColumn Header="Value" 
                                    Binding="{Binding Value}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10">
            <Button Content="&amp;OK" 
                    Command="{Binding OkCommand}"
                    Width="75" Margin="5"/>
            <Button Content="&amp;Cancel" 
                    Command="{Binding CancelCommand}"
                    Width="75" Margin="5"/>
            <Button Content="&amp;Apply" 
                    Command="{Binding ApplyCommand}"
                    Width="75" Margin="5"/>
            <Button Content="Change &amp;Unit Weight" 
                    Command="{Binding ChangeUnitWeightCommand}"
                    Width="128" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
