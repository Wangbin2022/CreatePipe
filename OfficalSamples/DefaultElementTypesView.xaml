<Page x:Class="CreatePipe.OfficalSamples.DefaultElementTypesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewModels="CreatePipe.OfficalSamples.DefaultElementTypesView"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="600"
      Title="默认元素类型管理器"
      xmlns:conv="clr-namespace:CreatePipe.Form.Converters">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- 类型候选项模板选择器 -->
        <DataTemplate x:Key="ComboBoxTemplate">
            <ComboBox ItemsSource="{Binding Candidates}"
                      SelectedItem="{Binding DefaultType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      DisplayMemberPath="Name"
                      IsSynchronizedWithCurrentItem="True"
                      VirtualizingPanel.ScrollUnit="Pixel">
                <ComboBox.ToolTip>
                    <ToolTip Content="{Binding SelectedItem.Name, RelativeSource={RelativeSource Self}}"/>
                </ComboBox.ToolTip>
            </ComboBox>
        </DataTemplate>

        <!-- 加载动画样式 -->
        <Style x:Key="LoadingOverlayStyle" TargetType="Border">
            <Setter Property="Background" Value="#80FFFFFF"/>
            <!--<Setter Property="Child">
                <Setter.Value>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="加载中..." FontSize="16" FontWeight="Bold"/>
                        <ProgressBar IsIndeterminate="True" Height="15" Width="200" Margin="0,10"/>
                    </StackPanel>
                </Setter.Value>
            </Setter>-->
        </Style>
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <ToolBar Grid.Row="0" Margin="5">
            <Button Command="{Binding RefreshCommand}" ToolTip="刷新数据">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/refresh.png" Width="16" Height="16" Margin="2"/>
                    <TextBlock Text="刷新" Margin="2,0,5,0"/>
                </StackPanel>
            </Button>
            <Separator/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="10,0"/>
        </ToolBar>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="1" 
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding TypeGroups}"
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  AlternatingRowBackground="#F5F5F5"
                  RowHeight="35"
                  Margin="10">

            <DataGrid.Resources>
                <!-- 进度条样式 -->
                <Style TargetType="ProgressBar">
                    <Setter Property="Height" Value="4"/>
                    <Setter Property="VerticalAlignment" Value="Top"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <!-- 类型组名称列 -->
                <DataGridTextColumn Header="元素类型组" 
                                    Binding="{Binding GroupName}"
                                    Width="180"
                                    IsReadOnly="True"
                                    FontWeight="Bold"/>

                <!-- 默认类型选择列 -->
                <DataGridTemplateColumn Header="默认类型" 
                                        Width="*"
                                        CellTemplate="{StaticResource ComboBoxTemplate}"/>

                <!-- 状态指示列 -->
                <DataGridTextColumn Header="状态" 
                                    Binding="{Binding DefaultType.Name, FallbackValue=未设置}"
                                    Width="200"
                                    IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="提示：修改默认类型后会自动应用到新建元素"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding TypeGroups.Count, StringFormat='共 {0} 个类型组'}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- 加载遮罩层 -->
        <Border Grid.RowSpan="3" 
                Style="{StaticResource LoadingOverlayStyle}"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</Page>
