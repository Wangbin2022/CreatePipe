<Window x:Class="CreatePipe.OfficalSamples.SelectionDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
        Title="选择工具" Height="200" Width="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        xmlns:conv="clr-namespace:CreatePipe.Form.Converters">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 元素信息 -->
        <Border Grid.Row="0" Background="#F0F0F0" Margin="0,0,0,10" Padding="5">
            <TextBlock Text="{Binding Manager.ElementInfo}" 
                       FontWeight="Bold"/>
        </Border>

        <!-- 点信息 -->
        <Border Grid.Row="1" Background="#F0F0F0" Margin="0,0,0,15" Padding="5">
            <TextBlock Text="{Binding Manager.PointInfo}" 
                       FontWeight="Bold"/>
        </Border>

        <!-- 按钮 -->
        <Button Grid.Row="2" 
                Content="拾取元素" 
                Command="{Binding PickElementCommand}"
                Height="30" Margin="0,5"/>

        <Button Grid.Row="3" 
                Content="移动到..." 
                Command="{Binding MoveToCommand}"
                Height="30" Margin="0,5"
                Background="LightBlue"/>

        <Button Grid.Row="4" 
                Content="关闭" 
                Command="{Binding CloseCommand}"
                Height="30" Margin="0,10,0,0"
                IsCancel="True"/>

        <!-- 处理中覆盖层 -->
        <Border Background="#CCFFFFFF" 
                Grid.RowSpan="5"
                Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="处理中..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
