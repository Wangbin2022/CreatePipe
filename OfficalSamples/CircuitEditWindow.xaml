<Window x:Class="CreatePipe.OfficalSamples.CircuitEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
   Title="编辑电路" Height="200" Width="300"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="请选择编辑操作：" Margin="0,0,0,15"/>

        <GroupBox Grid.Row="1" Header="操作选项" Margin="0,0,0,15">
            <StackPanel Margin="15">
                <RadioButton Content="添加设备到电路" 
                             IsChecked="{Binding IsAddSelected}"
                             Margin="0,5"/>
                <RadioButton Content="从电路移除设备" 
                             IsChecked="{Binding IsRemoveSelected}"
                             Margin="0,5"/>
                <RadioButton Content="选择配电盘" 
                             IsChecked="{Binding IsSelectPanelSelected}"
                             Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Command="{Binding OkCommand}"
                    Width="75" Margin="5" IsDefault="True"/>
            <Button Content="取消" Command="{Binding CancelCommand}"
                    Width="75" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
