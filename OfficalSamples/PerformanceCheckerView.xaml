<Window x:Class="CreatePipe.OfficalSamples.PerformanceCheckerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
    Title="性能规则控制器" Height="500" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 规则列表 -->
        <DataGrid Grid.Row="0" 
                  ItemsSource="{Binding Rules}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <!-- 选择复选框列 -->
                <DataGridCheckBoxColumn Header="运行" 
                                        Binding="{Binding IsSelected}"
                                        Width="50"/>

                <!-- 规则名称列 -->
                <DataGridTextColumn Header="规则名称" 
                                    Binding="{Binding RuleName}"
                                    Width="*"
                                    IsReadOnly="True"/>

                <!-- 自定义规则标识列 -->
                <DataGridTemplateColumn Header="自定义规则" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsOurRule}" 
                                      IsEnabled="False"
                                      HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,15,0,0">

            <Button Content="全选" 
                    Command="{Binding SelectAllCommand}"
                    Style="{StaticResource ActionButton}"/>

            <Button Content="取消全选" 
                    Command="{Binding DeselectAllCommand}"
                    Style="{StaticResource ActionButton}"/>

            <Button Content="运行选中规则" 
                    Command="{Binding RunTestsCommand}"
                    Style="{StaticResource ActionButton}"
                    Background="LightGreen"/>

            <Button Content="取消" 
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource ActionButton}"/>
        </StackPanel>

        <!-- 执行中覆盖层 -->
        <Border Background="#CCFFFFFF" 
                Visibility="{Binding IsExecuting, Converter={StaticResource BoolToVis}}"
                Grid.RowSpan="2">
            <TextBlock Text="执行中..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontSize="16"
                       FontWeight="Bold"/>
        </Border>
    </Grid>
</Window>
