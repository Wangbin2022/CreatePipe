<Window x:Class="CreatePipe.OfficalSamples.CreateOrthogonalGridView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
      Title="创建正交轴线" 
        Height="520" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
         xmlns:conv="clr-namespace:CreatePipe.Form.Converters">


    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- 验证错误模板 -->
        <ControlTemplate x:Key="ValidationErrorTemplate">
            <StackPanel Orientation="Horizontal">
                <AdornedElementPlaceholder x:Name="adorner"/>
                <TextBlock Text="⚠️" Foreground="Red" Margin="5,0,0,0" 
                           ToolTip="{Binding ElementName=adorner, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"
                           FontSize="14"/>
            </StackPanel>
        </ControlTemplate>

        <!-- 数字验证样式 -->
        <Style x:Key="NumberTextBoxStyle" TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationErrorTemplate}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- 分组框样式 -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15,10">
            <TextBlock Text="📐 正交轴线创建工具" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>

        <!-- 原点设置区域 -->
        <GroupBox Grid.Row="1" Header="📍 原点设置">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="X坐标：" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Column="1" Text="{Binding XOrigin, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource NumberTextBoxStyle}" Margin="5"/>

                <TextBlock Grid.Column="2" Text="Y坐标：" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Column="3" Text="{Binding YOrigin, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource NumberTextBoxStyle}" Margin="5"/>
            </Grid>
        </GroupBox>

        <!-- X方向轴线设置 -->
        <GroupBox Grid.Row="2" Header="↔️ X方向轴线（水平）">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="数量：" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding XCount, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource NumberTextBoxStyle}" Margin="5"/>

                <TextBlock Grid.Column="2" Text="间距：" VerticalAlignment="Center"/>
                <TextBox Grid.Column="3" Text="{Binding XSpacing, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource NumberTextBoxStyle}" Margin="5"/>

                <TextBlock Grid.Row="1" Text="起始标签：" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         Text="{Binding XFirstLabel, UpdateSourceTrigger=PropertyChanged}"
                         Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="2" Text="气泡位置：" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="3" 
                          ItemsSource="{Binding BubbleLocationOptions}"
                          SelectedValue="{Binding XBubbleLoc}"
                          SelectedValuePath="Value"
                          DisplayMemberPath="DisplayName"
                          Margin="5"/>
            </Grid>
        </GroupBox>

        <!-- Y方向轴线设置 -->
        <GroupBox Grid.Row="3" Header="↕️ Y方向轴线（垂直）">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="数量：" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding YCount, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource NumberTextBoxStyle}" Margin="5"/>

                <TextBlock Grid.Column="2" Text="间距：" VerticalAlignment="Center"/>
                <TextBox Grid.Column="3" Text="{Binding YSpacing, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource NumberTextBoxStyle}" Margin="5"/>

                <TextBlock Grid.Row="1" Text="起始标签：" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         Text="{Binding YFirstLabel, UpdateSourceTrigger=PropertyChanged}"
                         Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="2" Text="气泡位置：" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="3" 
                          ItemsSource="{Binding BubbleLocationOptions}"
                          SelectedValue="{Binding YBubbleLoc}"
                          SelectedValuePath="Value"
                          DisplayMemberPath="DisplayName"
                          Margin="5"/>
            </Grid>
        </GroupBox>

        <!-- 状态栏和按钮区域 -->
        <Border Grid.Row="4" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 状态消息 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📌 " FontSize="12"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#666"/>
                    <ProgressBar Width="80" Height="12" 
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Margin="10,0,0,0"/>
                </StackPanel>

                <!-- 按钮 -->
                <Button Grid.Column="1" Content="取消" 
                        Command="{Binding CancelCommand}"
                        Width="80" Height="30" Margin="5"
                        Background="#E0E0E0"/>

                <Button Grid.Column="2" Content="✅ 创建轴线" 
                        Command="{Binding CreateCommand}"
                        Width="100" Height="30" Margin="5"
                        Background="#27AE60" Foreground="White"
                        IsEnabled="{Binding CanCreate}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
