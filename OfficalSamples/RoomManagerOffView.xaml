<Window x:Class="CreatePipe.OfficalSamples.RoomManagerOffView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
        Title="房间信息管理" Height="550" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        xmlns:conv="clr-namespace:CreatePipe.Form.Converters">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 房间列表 -->
        <GroupBox Grid.Row="0" Grid.ColumnSpan="2" Header="房间信息" Margin="0,0,0,10">
            <DataGrid ItemsSource="{Binding Rooms}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserSortColumns="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="120"/>
                    <DataGridTextColumn Header="编号" Binding="{Binding Number}" Width="80"/>
                    <DataGridTextColumn Header="标高" Binding="{Binding LevelName}" Width="100"/>
                    <DataGridTextColumn Header="部门" Binding="{Binding Department}" Width="100"/>
                    <DataGridTextColumn Header="面积" Binding="{Binding Area}" Width="80"/>
                    <DataGridCheckBoxColumn Header="有标记" Binding="{Binding HasTag}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- 部门统计 -->
        <GroupBox Grid.Row="1" Grid.ColumnSpan="2" Header="部门面积统计" Margin="0,0,0,10">
            <DataGrid ItemsSource="{Binding Departments}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Height="150">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="部门" Binding="{Binding DepartmentName}" Width="150"/>
                    <DataGridTextColumn Header="房间数量" Binding="{Binding RoomsAmount}" Width="100"/>
                    <DataGridTextColumn Header="总面积" Binding="{Binding TotalArea}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" Grid.Column="0" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left">
            <Button Content="添加标记" Command="{Binding AddTagsCommand}"
                    Style="{StaticResource ActionButton}"
                    IsEnabled="{Binding CanExecute}"/>
            <Button Content="重新编号" Command="{Binding ReorderCommand}"
                    Style="{StaticResource ActionButton}"
                    IsEnabled="{Binding CanExecute}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="导出统计" Command="{Binding ExportCommand}"
                    Style="{StaticResource ActionButton}"
                    IsEnabled="{Binding CanExecute}"/>
            <Button Content="关闭" Command="{Binding CloseCommand}"
                    Style="{StaticResource ActionButton}"
                    IsCancel="True"/>
        </StackPanel>

        <!-- 处理中覆盖层 -->
        <Border Background="#CCFFFFFF" 
                Grid.RowSpan="3" Grid.ColumnSpan="2"
                Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="处理中..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontSize="16"/>
        </Border>
    </Grid>
</Window>
