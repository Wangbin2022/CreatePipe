<Window x:Class="CreatePipe.OfficalSamples.PatternManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
    Title="图案管理器" Height="550" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        >

    <Window.Resources>
        <!-- 选中状态样式 -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0078D4"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标签页 -->
        <TabControl Grid.Row="0" SelectedIndex="{Binding SelectedTabIndex}">
            <!-- 填充图案标签页 -->
            <TabItem Header="填充图案">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 图案列表 -->
                    <ListBox Grid.Row="0" 
                             ItemsSource="{Binding FillPatterns}"
                             SelectedItem="{Binding SelectedFillPattern}"
                             DisplayMemberPath="PreviewText"
                             BorderBrush="#CCCCCC"
                             BorderThickness="1">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}"/>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <!-- 按钮区域 -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="应用到表面" 
                                Command="{Binding ApplyToSurfaceCommand}"
                                Width="110" Height="30" Margin="0,0,10,0"/>
                        <Button Content="应用到切割面" 
                                Command="{Binding ApplyToCutSurfaceCommand}"
                                Width="110" Height="30"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- 线型图案标签页 -->
            <TabItem Header="线型图案">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 图案列表 -->
                    <ListBox Grid.Row="0" 
                             ItemsSource="{Binding LinePatterns}"
                             SelectedItem="{Binding SelectedLinePattern}"
                             DisplayMemberPath="PreviewText"
                             BorderBrush="#CCCCCC"
                             BorderThickness="1"/>

                    <!-- 按钮区域 -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="应用到网格线" 
                                Command="{Binding ApplyToGridsCommand}"
                                Width="110" Height="30"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="1" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <ProgressBar Width="100" Height="15" 
                             IsIndeterminate="{Binding IsProcessing}"
                             Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- 底部按钮栏 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="创建填充图案" 
                    Command="{Binding CreateFillPatternCommand}"
                    Width="110" Height="30" Margin="0,0,10,0"/>
            <Button Content="导入复杂图案" 
                    Command="{Binding CreateComplexFillPatternCommand}"
                    Width="110" Height="30" Margin="0,0,10,0"/>
            <Button Content="创建线型" 
                    Command="{Binding CreateLinePatternCommand}"
                    Width="90" Height="30" Margin="0,0,10,0"/>
            <Button Content="关闭" 
                    Command="{Binding CancelCommand}"
                    Width="70" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
