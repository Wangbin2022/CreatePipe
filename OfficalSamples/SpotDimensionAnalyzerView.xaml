<Window x:Class="CreatePipe.OfficalSamples.SpotDimensionAnalyzerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.OfficalSamples"
      Title="点标注信息分析器" 
        Height="650" Width="850"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
         xmlns:conv="clr-namespace:CreatePipe.Form.Converters">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- 分组框样式 -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- 列表视图样式 -->
        <Style TargetType="ListView">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFD9D9D9"/>
        </Style>

        <!-- 加载指示器样式 -->
        <Style x:Key="LoadingOverlayStyle" TargetType="Border">
            <Setter Property="Background" Value="#80FFFFFF"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <ToolBar Grid.Row="0" Height="40">
            <Label Content="视图:" VerticalAlignment="Center" Margin="5,0"/>
            <ComboBox x:Name="ViewsComboBox" 
                      Width="250"
                      ItemsSource="{Binding Views}"
                      SelectedItem="{Binding SelectedView}"
                      DisplayMemberPath="ViewName"
                      IsSynchronizedWithCurrentItem="True"
                      Margin="5,0"/>

            <Separator/>

            <Button Content="刷新" Command="{Binding RefreshCommand}" 
                    Margin="10,0" Padding="10,5"/>
        </ToolBar>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：点标注列表 -->
            <GroupBox Header="点标注列表" Grid.Column="0">
                <ListView x:Name="SpotDimensionsListView"
                          ItemsSource="{Binding SpotDimensions}"
                          SelectedItem="{Binding SelectedSpotDimension}"
                          SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" Width="60" 
                                            DisplayMemberBinding="{Binding Id.IntegerValue}"/>
                            <GridViewColumn Header="显示名称" Width="200" 
                                            DisplayMemberBinding="{Binding DisplayName}"/>
                            <GridViewColumn Header="类别" Width="100" 
                                            DisplayMemberBinding="{Binding Category}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>

            <!-- 分隔线 -->
            <GridSplitter Grid.Column="1" Width="2" HorizontalAlignment="Stretch"/>

            <!-- 右侧：参数信息 -->
            <GroupBox Header="参数信息" Grid.Column="2">
                <DataGrid x:Name="ParametersGrid"
                          ItemsSource="{Binding Parameters}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#FFF5F5F5"
                          Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="参数名称" 
                                            Binding="{Binding ParameterName}"
                                            Width="180"/>
                        <DataGridTextColumn Header="参数值" 
                                            Binding="{Binding ParameterValue}"
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="点标注分析工具 v1.0"/>
            </StatusBarItem>
        </StatusBar>

        <!-- 加载遮罩 -->
        <Border Grid.RowSpan="3" Style="{StaticResource LoadingOverlayStyle}"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Text="加载中..." Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
