<Window x:Class="CreatePipe.OfficalSamples.WallFromBeamsCreatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       Title="从梁轮廓创建墙体" Height="400" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- 验证状态颜色转换器 -->
        <Style x:Key="ValidationStatusStyle" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsProfileValid}" Value="True">
                    <Setter Property="Foreground" Value="Green"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsProfileValid}" Value="False">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="从梁轮廓创建墙体" FontSize="16" FontWeight="Bold" 
                   Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <!-- 梁信息 -->
        <Border Grid.Row="1" Background="#E8F0FE" CornerRadius="5" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="选中的梁数量:" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding SelectedBeamCount}" 
                           FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- 轮廓验证状态 -->
        <Border Grid.Row="2" Background="#F5F5F5" CornerRadius="5" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="轮廓验证状态:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding ProfileValidationMessage}" 
                           Style="{StaticResource ValidationStatusStyle}"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- 墙体参数设置 -->
        <GroupBox Grid.Row="3" Header="墙体参数" Margin="0,0,0,15">
            <StackPanel Margin="10">
                <!-- 墙体类型 -->
                <TextBlock Text="墙体类型:" Margin="0,5,0,2"/>
                <ComboBox ItemsSource="{Binding Settings.AvailableWallTypes}" 
                          SelectedItem="{Binding Settings.SelectedWallType}"
                          DisplayMemberPath="Name"
                          IsEnabled="{Binding IsProfileValid}"/>

                <!-- 标高 -->
                <TextBlock Text="基准标高:" Margin="0,10,0,2"/>
                <ComboBox ItemsSource="{Binding Settings.AvailableLevels}" 
                          SelectedItem="{Binding Settings.TargetLevel}"
                          DisplayMemberPath="Name"
                          IsEnabled="{Binding IsProfileValid}"/>

                <!-- 结构墙选项 -->
                <CheckBox Content="创建结构墙" 
                          IsChecked="{Binding Settings.IsStructural}"
                          IsEnabled="{Binding IsProfileValid}"
                          Margin="0,10,0,0"/>
            </StackPanel>
        </GroupBox>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="4" Margin="0,0,0,10">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <ProgressBar Width="80" Height="15" IsIndeterminate="{Binding IsProcessing}"
                             Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="创建墙体" 
                    Command="{Binding CreateWallCommand}"
                    Width="90" Height="30" Margin="0,0,10,0"
                    IsDefault="True"
                    IsEnabled="{Binding CanCreate}"/>
            <Button Content="取消" 
                    Command="{Binding CancelCommand}"
                    Width="75" Height="30"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
