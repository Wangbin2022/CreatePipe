<Window x:Class="CreatePipe.OfficalSamples.PatternCreateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="创建填充图案" Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="图案名称:" Margin="0,5"/>
        <TextBox Grid.Row="1" Name="PatternNameBox" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="线间距 (mm):" Margin="0,5"/>
        <TextBox Grid.Row="3" Name="SpacingBox" Text="10" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="旋转角度 (度):" Margin="0,5"/>
        <TextBox Grid.Row="5" Name="AngleBox" Text="0" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="75" Height="30" 
                    Click="OnOkClick" IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="取消" Width="75" Height="30" 
                    Click="OnCancelClick" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
