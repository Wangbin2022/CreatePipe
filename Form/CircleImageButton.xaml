<UserControl x:Class="CreatePipe.Form.CircleImageButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CreatePipe.Form" x:Name="Root">
    <UserControl.Template>
        <ControlTemplate TargetType="UserControl">
            <!-- 【关键接力 1】在这里加上 Content="{TemplateBinding Content}" -->
            <!-- 把 UserControl 接收到的文字，喂给内部的 Button -->
            <Button Content="{TemplateBinding Content}"
                    Click="InnerButton_Click" 
                    Command="{Binding Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding CommandParameter, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Foreground="{TemplateBinding Foreground}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- 1. 按钮背景层 -->
                            <Ellipse x:Name="bg" 
                                     Fill="{TemplateBinding Background}" 
                                     Stroke="{TemplateBinding BorderBrush}" 
                                     StrokeThickness="{Binding BorderThickness.Left, RelativeSource={RelativeSource TemplatedParent}}"/>

                            <!-- 2. 图片层 -->
                            <!-- 为了绝对的安全，将找图片的方式改为了 AncestorType（向上找UserControl），杜绝任何找不到报错 -->
                            <Ellipse Margin="{TemplateBinding Padding}">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- 3. 【关键接力 2】内容层 -->
                            <!-- 这里什么绑定都不用写，ContentPresenter会自动显示当前Button的Content -->
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Grid>
                        <!-- 鼠标交互反馈 -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
