<Window x:Class="CreatePipe.Form.RevitStylePopup.RevitStylePopupView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="ToastWindow" 
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        WindowStartupLocation="CenterScreen"
        Width="320" Height="130">
    <Window.Resources>
        <sys:Int32 x:Key="DelaySeconds">1</sys:Int32>
    </Window.Resources>
    <Border Background="#FF2B2B2B"
            BorderBrush="#FF555555"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <!-- 标题栏 -->
            <Border Grid.Row="0"
                    Background="#f3f3f3"
                    BorderBrush="#f3f3f3"
                    BorderThickness="0,0,0,1"
                    CornerRadius="4,4,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               x:Name="MessageTitle" 
                               Foreground="#555555"
                               FontSize="14"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"/>
                </Grid>
            </Border>
            <!-- 内容区域 -->
            <Border Grid.Row="1" Padding="12" Background="#ffffff" >
                <TextBlock x:Name="MessageTextBlock" TextWrapping="Wrap" Foreground="#224ca3" FontSize="16" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Row="2" Padding="6"  Background="#f3f3f3"
                    BorderBrush="#f3f3f3" CornerRadius="0,0,9,9">
                <TextBlock Text="命令已执行完毕，本窗口将于2秒后自动关闭" Margin="6,0"/>
            </Border>
        </Grid>
    </Border>
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation
                        Storyboard.TargetProperty="Opacity"
                        From="1.0" To="0.0"
                        Duration="0:0:1"
                        BeginTime="0:0:2"
                        Completed="CloseWindow"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
