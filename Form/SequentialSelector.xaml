<UserControl x:Class="CreatePipe.Form.SequentialSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="250">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="3">
        <StackPanel>
            <TextBlock Text="{Binding InstructionText}" TextWrapping="Wrap" HorizontalAlignment="Center"/>
            <TextBlock HorizontalAlignment="Center" Margin="0,5,0,0">
                <Run Text="已选数量："/>
                <Run Text="{Binding SelectedCount}" FontWeight="Bold" FontSize="16"/>
            </TextBlock>
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Margin="0,0,5,0" 
                        Content="{Binding StartButtonText}" 
                        Command="{Binding ToggleSelectionCommand}"/>
                <Button Grid.Column="1" Margin="5,0,0,0" 
                        Content="完成选择" 
                        Command="{Binding FinishSelectionCommand}"
                        IsEnabled="{Binding IsFinishButtonEnabled}"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
