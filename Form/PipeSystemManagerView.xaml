<Window x:Class="CreatePipe.Form.PipeSystemManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.Form"
        mc:Ignorable="d"
        Title="管道系统管理" 
        Height="450" Width="720"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:vm="clr-namespace:CreatePipe.models"
        xmlns:converter ="clr-namespace:CreatePipe.Utils">
    <Window.Resources>
        <converter:ColorConvertHelper x:Key="colorConverter"/>
        <vm:BackgroundToForegroundConverter  x:Key="BackgroundToForegroundConverter"/>
        <vm:LastSystemTextColor x:Key="LastSystemText"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBox Width="200" VerticalAlignment="Center"  
                     Text="{Binding Keyword,UpdateSourceTrigger=PropertyChanged}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="TextChanged">
                        <i:InvokeCommandAction Command="{Binding QueryELementCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </TextBox>
        </StackPanel>
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <TextBlock Text="{Binding PipingSystemCount,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
            <Button Width="66" Margin="2" Content="选系统全部" Command="{Binding SelectSystemCommand}" 
                    CommandParameter="{Binding SelectedItems, ElementName=dataGrid}"
                    Click="btn_OK_Click"/>
            <Button Width="66" Margin="2" Content="增加系统" x:Name="Btn_Add" Click="Btn_Add_Click"/>
            <Button Width="66" Margin="2" Content="删除系统"  
                    Command="{Binding DeleteELementCommand}"
CommandParameter="{Binding SelectedItems, ElementName=dataGrid}"/>
        </StackPanel>
        <StackPanel  Grid.Row="1">
            <DataGrid x:Name="dataGrid" RowHeight="35"  
                      HorizontalAlignment="Stretch"
                      AutoGenerateColumns="False" CanUserAddRows="False"
                      SelectionUnit="FullRow" EnableColumnVirtualization="False"
                      GridLinesVisibility="None" Height="350"  Width="auto"
                      SelectionMode="Extended" ItemsSource="{Binding PipeSystemEntitys}">
                <DataGrid.Resources>
                    <Style TargetType="DataGridCell" x:Key="dataGridCellStyle">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="LightGray"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Lightblue"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <TextBlock TextAlignment="Center" >
                           <ContentPresenter />
                                    </TextBlock>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style x:Key="TextVerticalCenter" TargetType="{x:Type TextBlock}">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.CellStyle>
                    <StaticResource ResourceKey="dataGridCellStyle"/>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="名称" Width="80" SortMemberPath="SystemName" 
                      Binding="{Binding SystemName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="False" ElementStyle="{StaticResource TextVerticalCenter}"/>
                    <DataGridTextColumn Header="缩写" Width="35" SortMemberPath="Abbreviation" CanUserResize="False"
                      Binding="{Binding Abbreviation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="False" ElementStyle="{StaticResource TextVerticalCenter}"/>
                    <DataGridTextColumn Header="数量统计" Width="50" SortMemberPath="SingleSystemElementCount" 
                      Binding="{Binding SingleSystemElementCount, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" ElementStyle="{StaticResource TextVerticalCenter}"/>
                    <DataGridTemplateColumn Header="线颜色" Width="80"
                                            SortMemberPath="ColorName">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button  BorderBrush="Transparent" Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window},Path=DataContext.SetColorCommand}" CommandParameter="{Binding pipingSystemType}" Background="Transparent">
                                    <TextBlock Text="{Binding ColorName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"    Foreground="{Binding LineColor,Converter={StaticResource colorConverter}}" VerticalAlignment="Center"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="线宽" Width="45">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <ComboBox ItemsSource="{Binding LineWeights}"   SelectedValue="{Binding LineWeight,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  VerticalContentAlignment="Center" Height="28"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="线型" Width="66">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <ComboBox x:Name="cb_Type"      ItemsSource="{Binding LinePatternElementInfos}"        DisplayMemberPath="Name"  SelectedItem="{Binding LinePatternElem,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  VerticalContentAlignment="Center" Height="28"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="内置系统" Width="65" SortMemberPath="MEPSystemClass" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border>
                                    <Label  Content="{Binding MEPSystemClass, Mode=OneWay}"                                            VerticalContentAlignment="Center" Padding="0" Height="28" Background="LightGray"  Foreground="{Binding pipingSystemType, Converter={StaticResource LastSystemText}}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="材质颜色" Width="80"
                                            SortMemberPath="Name" >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="80"
                                Height="20"
                                Background="{Binding Color,Converter={StaticResource colorConverter}}"
                                BorderBrush="Gray"
                                BorderThickness="1"
                                CornerRadius="5">
                                    <TextBlock Text="{Binding ColorValue}"
               Foreground="{Binding Background, RelativeSource={RelativeSource AncestorType=Border}, Converter={StaticResource BackgroundToForegroundConverter}}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="材质名称" Width="100"
                             SortMemberPath="Name" >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="100"  
                 Background="Transparent" BorderBrush="Transparent"
                 Content="{Binding Name}" 
                        Command="{Binding DataContext.WindowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!--<DataGridTextColumn Header="材质名称" Width="100" SortMemberPath="Name" 
  Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" ElementStyle="{StaticResource TextVerticalCenter}"/>-->
                    <DataGridTemplateColumn Header="更改操作" CanUserResize="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <!--<Button Content="删除系统" Width="auto" Command="{Binding DataContext.DeleteELementCommand2, RelativeSource={RelativeSource AncestorType=DataGrid}}"
     CommandParameter="{Binding}" Height="28" HorizontalAlignment="Stretch"/>-->
                                <Button Content="添加保温" Width="auto" Command="{Binding DataContext.AddInsulationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
     CommandParameter="{Binding}" Height="28" HorizontalAlignment="Stretch"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!--<DataGridCheckBoxColumn Header="测试" Width="40">
                        <DataGridCheckBoxColumn.HeaderTemplate>
                            <DataTemplate>
                                <CheckBox x:Name="ckbSelectedAll" Content="全选" 
                                          Checked="ckbSelectedAll_Checked"
                                          Unchecked="ckbSelectedAll_Unchecked"
                                          IsChecked="False"/>
                            </DataTemplate>
                        </DataGridCheckBoxColumn.HeaderTemplate>
                    </DataGridCheckBoxColumn>-->
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <DockPanel Grid.Row="2">
            <Button  Content="确定并退出" Height="20" x:Name="btn_OK"
                     HorizontalAlignment="Stretch" Click="btn_OK_Click"/>
        </DockPanel>
    </Grid>
</Window>
