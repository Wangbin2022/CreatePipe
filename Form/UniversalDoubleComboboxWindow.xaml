<Window x:Class="CreatePipe.Form.UniversalDoubleComboboxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe.Form"
        mc:Ignorable="d"
        Title="UniversalDoubleComboboxWindow" Height="360" Width="240"
                WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- 第一个下拉框 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="{Binding Header1}" Foreground="Gray" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding ItemsSource1}" 
                      SelectedItem="{Binding SelectedItem1}" Height="26"/>
        </StackPanel>
        <!-- 第二个下拉框 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="{Binding Header2}" Foreground="Gray" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding ItemsSource2}" 
                      SelectedItem="{Binding SelectedItem2}" Height="26"/>
        </StackPanel>
        <!-- 按钮区 -->
        <Button Grid.Row="3" Content="确认并退出" x:Name="btn_OK" IsDefault="True"
                Height="26" FontSize="16"   Click="BtnOK_Click" />
        <!--<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="Auto" Height="26" Margin="5" Click="BtnOK_Click"/>
            --><!--<Button Content="取消" Width="70" Height="26" Click="BtnCancel_Click"/>--><!--
        </StackPanel>-->
    </Grid>
</Window>
