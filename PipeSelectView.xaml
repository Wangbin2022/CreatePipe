<Window x:Class="CreatePipe.PipeSelectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatePipe"
        mc:Ignorable="d"
        Title="选取管径" 
        Height="300" Width="240"
        WindowStartupLocation="CenterScreen"
        xmlns:form="clr-namespace:CreatePipe.Form">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="10,15,10,0">
            <TextBlock FontWeight="Black" Text="管道系统名称"/>
            <TextBox Margin="0,2" Text="{Binding SelectedPipeSystem}" TextAlignment="Center" 
                  HorizontalAlignment="Stretch" IsReadOnly="True" x:Name="nm"/>
            <TextBlock FontWeight="Black" Text="管道管径 DN" Margin="0,10,0,2"/>
            <form:MultiSelectComboBox  ItemsSource="{Binding Items}" 
SelectedItems="{Binding SelectedItems}" 
ToolTip="{Binding Path=Text, RelativeSource={RelativeSource Self}}" />       
        </StackPanel>
        <Border Grid.Row="1" Margin="5" Padding="0,2,0,0"
              BorderThickness="0,1,0,0" BorderBrush="Gray">
            <Button  Content="确定修改并退出"  Command="{Binding ReturnSelectionCommand}" 
                   Click="Button_Click"/>
        </Border>
    </Grid>
</Window>
